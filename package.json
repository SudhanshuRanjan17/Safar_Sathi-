{
  "name": "safarsetu-prototype",
  "version": "1.0.0",
  "description": "SafarSetu Interactive Mobile Prototype",
  "main": "index.html",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0 --open",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}